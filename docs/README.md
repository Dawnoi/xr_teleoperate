# docs

项目文档按用途分层：

- `architecture/`：项目结构、模块边界、运行链路
- `runbooks/`：真机/仿真/设备/故障排查运行手册
- `datasets/`：数据集格式、采集、回放、导出说明
- `refactor/`：重构计划、迁移记录、历史 review
- `online_inference_*.md`：现有在线推理设计文档，后续可归入 `architecture/` 或 `inference/` 专题
